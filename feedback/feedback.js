get_action = function(form) 
{
    var input = document.getElementsByClassName("c-form__input")[0];
    if(input.value.length >= input.min) 
    {
        showSnackbar();

        //Send Feedback
        /*no function*/
    }
}